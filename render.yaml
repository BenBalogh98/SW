services:
  - type: web
    name: sw-backend
    runtime: docker
    rootDir: SWBackend
    dockerfilePath: Dockerfile.prod
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
