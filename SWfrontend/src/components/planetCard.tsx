import { PlanetCardProps } from "../interfaces/planetCardProps";
import Card from "./card";

export default class PlanetCard extends Card {
    constructor(props: PlanetCardProps) {
        super(props);
    }

    public render(): React.JSX.Element {
        return super.render();
    }
}